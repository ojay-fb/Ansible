---

- hosts: Proxmox_BS:Proxmox_VE:Proxmox_PDM
  become: true
  remote_user: root
  tasks:
    - name: update repository index
      apt:
        update_cache: yes
    - name: upgrade proxmox server
      apt:
        upgrade: dist
